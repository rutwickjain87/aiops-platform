# OpenRouter backend — used by planner_openrouter.py + tools_openrouter.py + memory_openrouter.py
# Uses the OpenAI Python SDK pointed at OpenRouter's OpenAI-compatible endpoint.
# Install with: uv pip install -r requirements_openrouter.txt
#
# Requires env var: OPENROUTER_API_KEY  (get free key at https://openrouter.ai/keys)
openai>=1.0.0
pydantic>=2.0.0
