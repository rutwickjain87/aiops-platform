# PR Security Reviewer — agent dependencies
# Install with: uv pip install -r requirements.txt

# LLM + agent framework
anthropic>=0.40.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0

# GitHub API client
PyGithub>=2.3.0

# SAST engine (semgrep CLI is invoked via subprocess — this installs the CLI)
semgrep>=1.70.0

# Observability
langsmith>=0.1.77          # LangSmith tracing (auto-traces LangChain calls)
prometheus-client>=0.20.0  # Prometheus metrics endpoint
python-json-logger>=2.0.0  # Structured JSON logging (shared observability lib)

# Testing
pytest>=8.0
pytest-cov>=5.0
