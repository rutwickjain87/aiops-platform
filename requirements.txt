# Root requirements — CI linting + test tooling only.
# Agent-specific deps live in agents/<name>/requirements_*.txt and are
# installed separately when running each agent.
ruff>=0.4.0
pytest>=8.0
pytest-cov>=5.0
pydantic>=2.0.0
